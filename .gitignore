# Neovim may fall back to the config directory when its normal state/log
# directory is not writable. These files are machine-local diagnostics.
nvim.log
*.log

# macOS metadata
.DS_Store

# Optional per-device language switches; see lua/config/languages.lua.
lua/config/languages_local.lua
