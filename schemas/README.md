# Bundled schemas

`vscode-tasks.json` is the SchemaStore VS Code tasks schema downloaded from:

https://www.schemastore.org/task.json

It is committed for offline, reproducible jsonls completion. Do not edit the
generated file manually. Run `:VSCodeTasksSchemaUpdate`, restart Neovim, and
review the Git diff when intentionally updating it.
